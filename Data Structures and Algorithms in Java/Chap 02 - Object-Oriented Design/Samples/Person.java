
public interface Person {
    public boolean equals(Person other);     // if two person are the same
    public String getName();                 // get the name of the person
    public int getAge();                     // get the age of the person
}
